export const CANVAS_CONFIG = {
  defaultZoom: 1,
  minZoom: 0.1,
  maxZoom: 8,
  zoomSensitivity: 0.0015,

  gridSize: 40,

  defaultSnapSize: 40,
  minSnapSize: 1,
  maxSnapSize: 200,

  maxDevicePixelRatio: 2,
} as const;
